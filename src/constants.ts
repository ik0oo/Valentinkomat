const BASE = '/valentinkomat';

export const ROUTES = {
  HOME: `${BASE}/home`,
  EDITOR: `${BASE}/editor`,
  PREVIEW: `${BASE}/preview`,
  SUCCESS: `${BASE}/success`,
  NEW: `${BASE}/card/:id`,
};

export const TEXT_AREA_MAX_LEN = 300;
